//reverse('hello') === 'olleh'

function reverse(str){
    const arr = str.split('');
    arr.reverse();
    return arr.join('');
}

function reverse(str){
    let reversed = '';
    for(let character of str){
        reversed = character + revresed;
    }
    return reversed;
}